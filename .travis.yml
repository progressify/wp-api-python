language: python
sudo: required
services: 
  - docker
python:
  - "2.7"
  - "3.4"
  - "nightly"
# command to install dependencies
install:
  - docker-compose up &
  - pip install .
  - pip install -r requirements-test.txt
  - sleep 30
# command to run tests
script:
  - pytest
